import React from 'react';

const JobDetails = ({JobDetails}) => {
    console.log(JobDetails)
    return (
        <div>
            {/* <h2>length:{JobDetails.id}</h2> */}
        </div>
    );
};

export default JobDetails;